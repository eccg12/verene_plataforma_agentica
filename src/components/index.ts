/**
 * Componentes próprios do KEPLER (sem UI kit externo).
 *
 * Barril de exportação — cada componente ganha seu arquivo e é reexportado aqui.
 * Componentes não carregam texto nem dado próprio: texto vem de `src/copy/strings.ts`,
 * dado vem de `src/data/`.
 */

export {}
